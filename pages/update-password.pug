Document-Title 重置密码

.account.update-password
  .account-wrapper
    img.account-logo(src=props.isvInfo.accountLogo)
    .account-form
      .account-form-module 重置密码
      SimpleForm(_ref=form states=state.states onSubmit=onSubmit layout="vertical")
      if state.errorMessage
        .account--error= state.errorMessage
      Button(type="primary" onClick=form.submit loading=form.isLoading() disabled=form.isLoading()) 确认重置
